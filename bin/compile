#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

BUILD_DIR=${1:-}

echo "-----> generate credentials file"
mkdir -p $BUILD_DIR/.profile.d

echo "-----> echo ${JSON_CREDENTIALS@Q} -> /app/${JSON_CREDENTIALS_NAME}.json"
# chmod +x $BUILD_DIR/.profile.d/${JSON_CREDENTIALS_NAME}.sh

echo "-----> generate finish"