#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

BUILD_DIR=${1:-}
CACHE_DIR=${2:-}
ENV_DIR=${3:-}

echo "--> ${BUILD_DIR}"
echo "--> ${CACHE_DIR}"
echo "--> ${ENV_DIR}"
echo "finish"